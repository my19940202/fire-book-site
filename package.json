{
  "name": "fire-book-site",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
